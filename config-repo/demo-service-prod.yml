server:
  port: 8093
  

druid:
    url: jdbc:mysql://localhost:3306/sjd?useUnicode=true&zeroDateTimeBehavior=convertToNull&characterEncoding=utf-8
    username: root
    password: root
    initial-size: 1
    min-idle: 1
    max-active: 20
    test-on-borrow: true

mybatis:
    type-aliases-package: com.xbwl.*.entity
    mapper-locations: classpath*:/mapper/**/*Mapper.xml

mapper:
    not-empty: false
identity: MYSQL
